function letrasPosicao() {
    alert("Posição das letras do alfabeto");
    let letra = prompt("Digite uma letra (números não são aceitos):").toLowerCase();


    switch (letra) {
        case "a":
            alert(letra.toUpperCase() + " é a 1° letra do alfabeto.");
            break;
        case "b":
            alert(letra.toUpperCase() + " é a 2° letra do alfabeto.");
            break;
        case "c":
            alert(letra.toUpperCase() + " é a 3° letra do alfabeto.");
            break;
        case "d":
            alert(letra.toUpperCase() + " é a 4° letra do alfabeto.");
            break;
        case "e":
            alert(letra.toUpperCase() + " é a 5° letra do alfabeto.");
            break;
        case "f":
            alert(letra.toUpperCase() + " é a 6° letra do alfabeto.");
            break;
        case "g":
            alert(letra.toUpperCase() + " é a 7° letra do alfabeto.");
            break;
        case "h":
            alert(letra.toUpperCase() + " é a 8° letra do alfabeto.");
            break;
        case "i":
            alert(letra.toUpperCase() + " é a 9° letra do alfabeto.")
            break;
        case "j":
            alert(letra.toUpperCase() + " é a 10° letra do alfabeto.");
            break;
        case "k":
            alert(letra.toUpperCase() + " é a 11° letra do alfabeto.");
            break;
        case "l":
            alert(letra.toUpperCase() + " é a 12° letra do alfabeto");
            break;
        case "m":
            alert(letra.toUpperCase() + " é a 13° letra dp alfabeto.");
            break;
        case "n":
            alert(letra.toUpperCase() + " é a 14° letra do alfabeto.");
            break;
        case "o":
            alert(letra.toUpperCase() + " é a 15° letra dp alfabeto.");
            break;
        case "p":
            alert(letra.toUpperCase() + " é a 16° letra do alfabeto.");
            break;
        case "q":
            alert(letra.toUpperCase() + " é a 17° letra do alfabeto.");
            break;
        case "r":
            alert(letra.toUpperCase() + " é a 18° letra do alfabeto.");
            break;
        case "s":
            alert(letra.toUpperCase() + " é a 19° letra do alfabeto.");
            break;
        case "t":
            alert(letra.toUpperCase() + " é a 20° letra do alfabeto.");
            break;
        case "u":
            alert(letra.toUpperCase() + " é a 21° letra do alfabeto.");
            break;
        case "v":
            alert(letra.toUpperCase() + " é a 22° letra do alfabeto.");
            break;
        case "w":
            alert(letra.toUpperCase() + " é a 23° letra do alfabeto.");
            break;
        case "x":
            alert(letra.toUpperCase() + " é a 24° letra do alfabeto.");
            break;
        case "y":
            alert(letra.toUpperCase() + " é a 25° letra do alfabeto.");
            break;
        case "z":
            alert(letra.toUpperCase() + " é a 26° letra do alfabeto.");
            break;
        default:
            alert("Valor digitado não corresponde!");
            break;
    }
}
